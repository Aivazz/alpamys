import 'package:flutter/material.dart';
import 'package:uicons/uicons.dart';

class UIconHome extends StatelessWidget {
  final Color color;
  final double size;
  const UIconHome({super.key, this.color = Colors.black, this.size = 24});

  @override
  Widget build(BuildContext context) {
    return Icon(
      UIcons.regularRounded.home,
      color: color,
      size: size,
    );
  }
}

class UIconSparkles extends StatelessWidget {
  final Color color;
  final double size;
  const UIconSparkles({super.key, this.color = Colors.black, this.size = 24});

  @override
  Widget build(BuildContext context) {
    return Icon(
      UIcons.regularRounded.sparkles,
      color: color,
      size: size,
    );
  }
}

class UIconChat extends StatelessWidget {
  final Color color;
  final double size;
  const UIconChat({super.key, this.color = Colors.black, this.size = 24});

  @override
  Widget build(BuildContext context) {
    return Icon(
      UIcons.regularRounded.comment,
      color: color,
      size: size,
    );
  }
}

class UIconProfile extends StatelessWidget {
  final Color color;
  final double size;
  const UIconProfile({super.key, this.color = Colors.black, this.size = 24});

  @override
  Widget build(BuildContext context) {
    return Icon(
      UIcons.regularRounded.user,
      color: color,
      size: size,
    );
  }
}

class UIconPlans extends StatelessWidget {
  final Color color;
  final double size;
  const UIconPlans({super.key, this.color = Colors.black, this.size = 24});

  @override
  Widget build(BuildContext context) {
    return Icon(
      UIcons.regularRounded.calendar,
      color: color,
      size: size,
    );
  }
}

class UIconTraining extends StatelessWidget {
  final Color color;
  final double size;
  const UIconTraining({super.key, this.color = Colors.black, this.size = 24});

  @override
  Widget build(BuildContext context) {
    return Icon(
      UIcons.regularRounded.gym,
      color: color,
      size: size,
    );
  }
}

class UIconMarket extends StatelessWidget {
  final Color color;
  final double size;
  const UIconMarket({super.key, this.color = Colors.black, this.size = 24});

  @override
  Widget build(BuildContext context) {
    return Icon(
      UIcons.regularRounded.shopping_cart,
      color: color,
      size: size,
    );
  }
}

class UIconHeart extends StatelessWidget {
  final Color color;
  final double size;
  const UIconHeart({super.key, this.color = Colors.black, this.size = 24});

  @override
  Widget build(BuildContext context) {
    return Icon(
      UIcons.regularRounded.heart,
      color: color,
      size: size,
    );
  }
}

class UIconSettings extends StatelessWidget {
  final Color color;
  final double size;
  const UIconSettings({super.key, this.color = Colors.black, this.size = 24});

  @override
  Widget build(BuildContext context) {
    return Icon(
      UIcons.regularRounded.settings,
      color: color,
      size: size,
    );
  }
}

class UIconLogout extends StatelessWidget {
  final Color color;
  final double size;
  const UIconLogout({super.key, this.color = Colors.black, this.size = 24});

  @override
  Widget build(BuildContext context) {
    return Icon(
      UIcons.regularRounded.sign_out_alt,
      color: color,
      size: size,
    );
  }
}

class UIconClose extends StatelessWidget {
  final Color color;
  final double size;
  const UIconClose({super.key, this.color = Colors.black, this.size = 24});

  @override
  Widget build(BuildContext context) {
    return Icon(
      UIcons.regularRounded.cross,
      color: color,
      size: size,
    );
  }
}

class UIconMenu extends StatelessWidget {
  final Color color;
  final double size;
  const UIconMenu({super.key, this.color = Colors.black, this.size = 24});

  @override
  Widget build(BuildContext context) {
    return Icon(
      UIcons.regularRounded.menu_burger,
      color: color,
      size: size,
    );
  }
}

class UIconNotification extends StatelessWidget {
  final Color color;
  final double size;
  const UIconNotification({super.key, this.color = Colors.black, this.size = 24});

  @override
  Widget build(BuildContext context) {
    return Icon(
      UIcons.regularRounded.bell,
      color: color,
      size: size,
    );
  }
}

class UIconSearch extends StatelessWidget {
  final Color color;
  final double size;
  const UIconSearch({super.key, this.color = Colors.black, this.size = 24});

  @override
  Widget build(BuildContext context) {
    return Icon(
      UIcons.regularRounded.search,
      color: color,
      size: size,
    );
  }
}

class UIconEdit extends StatelessWidget {
  final Color color;
  final double size;
  const UIconEdit({super.key, this.color = Colors.black, this.size = 24});

  @override
  Widget build(BuildContext context) {
    return Icon(
      UIcons.regularRounded.edit,
      color: color,
      size: size,
    );
  }
}

class UIconAboneler extends StatelessWidget {
  final Color color;
  final double size;
  const UIconAboneler({super.key, this.color = Colors.black, this.size = 24});

  @override
  Widget build(BuildContext context) {
    return Icon(
      UIcons.regularRounded.ticket,
      color: color,
      size: size,
    );
  }
}
